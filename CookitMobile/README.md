1. 클론해서 가져오면 환경변수가 설정되어있지 않을 수 있음
   .env 파일 만들어서 환경변수 추가
------------
2. 프론트만 보는 경우라서 cd CookitMobile ,  npm install 해서 패키지 설치
3. npx expo start 할 때 인터넷을 꼭 확인(중요) -> 실행이 안되는 경우가 있음
--------
4. 매우 지저분한 상태. 구현을 우선 목표로 잡고 최적화는 구현 이후

