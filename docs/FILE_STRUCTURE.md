# Cookit 프로젝트 파일 구조도

## 📁 전체 프로젝트 구조

```
Cookit/
├── CookitMobile/          # 프론트엔드 (React Native + Expo)
└── Server/                # 백엔드 (Node.js + Express)
```

---

## 📱 프론트엔드 (CookitMobile)

```
CookitMobile/
├── App.js                 # 앱 진입점
├── app.config.js          # Expo 설정
├── babel.config.js        # Babel 설정 (Path Aliases)
├── tsconfig.json          # TypeScript 설정
├── package.json           # 의존성 관리
│
├── assets/                # 정적 리소스
│   ├── app_logo.png       # 앱 로고
│   ├── icon.png           # 앱 아이콘
│   └── splash.png         # 스플래시 화면
│
├── features/              # 기능별 모듈 (기능 모듈화)
│   ├── auth/              # 인증 모듈
│   │   ├── components/   # AuthNavigator, AuthScreen, GoogleSignInButton
│   │   ├── contexts/      # AuthContext (useAuth Hook)
│   │   └── index.ts       # 모듈 export
│   │
│   ├── recipe/            # 레시피 모듈
│   │   ├── components/    # RecipeCard, YouTubePlayer, YouTubeAnalysisModal
│   │   ├── contexts/      # AnalysisContext
│   │   ├── screens/       # HomeMain, RecipeMain, Summary, RecipeList, Search
│   │   ├── services/      # recipeService, recipeLikesApi
│   │   └── index.ts
│   │
│   ├── refrigerator/       # 냉장고 관리 모듈
│   │   ├── screens/       # Ingredients, ReceiptMain, SetupIngredients
│   │   ├── services/      # receiptItemsApi
│   │   └── index.ts
│   │
│   ├── community/         # 커뮤니티 모듈
│   │   ├── screens/       # CommunityMain, CommunityDetail, CommunityCreate
│   │   ├── services/      # postsApi, commentsApi, postLikesApi
│   │   └── index.ts
│   │
│   ├── profile/           # 프로필 모듈
│   │   ├── screens/       # ProfileMain, ProfileEdit, SettingsStack
│   │   ├── services/      # userApi
│   │   └── index.ts
│   │
│   └── navigation/        # 네비게이션 모듈
│       ├── HomeTab.tsx    # 하단 탭 네비게이터
│       └── index.ts
│
├── shared/                # 공유 리소스 (모든 모듈에서 사용)
│   ├── components/        # 공통 컴포넌트 (SearchInput, Sort, WheelDatePicker)
│   ├── lib/               # 공통 라이브러리
│   │   └── supabase.ts    # Supabase 클라이언트
│   ├── services/          # 공통 서비스
│   │   └── notificationService.js
│   └── types/             # 공통 타입 정의
│       └── auth.ts
│
└── android/               # Android 네이티브 코드
    └── app/
```

### 프론트엔드 주요 특징
- **기능별 모듈화**: `features/` 디렉토리에 기능별로 분리
- **공유 리소스**: `shared/` 디렉토리에 공통 컴포넌트/서비스 관리
- **Path Aliases**: `@features/*`, `@shared/*`, `@assets/*` 사용
- **TypeScript + JavaScript**: 점진적 타입 적용

---

## 🖥️ 백엔드 (Server)

```
Server/
├── app.js                 # Express 앱 진입점
├── package.json           # 의존성 관리
├── nodemon.json           # 개발 서버 설정
│
├── routes/                # API 라우트 정의
│   ├── auth.js            # 인증 API
│   ├── recipes.js          # 레시피 CRUD
│   ├── recommendations.js  # 추천 시스템
│   ├── recipeLikes.js     # 레시피 좋아요
│   ├── userPosts.js       # 커뮤니티 게시글
│   ├── comments.js        # 댓글 관리
│   ├── postLikes.js       # 게시글 좋아요
│   ├── receiptOcr.js      # 영수증 OCR
│   ├── receiptItems.js    # 냉장고 아이템 관리
│   ├── users.js           # 사용자 관리
│   ├── recipeCategories.js # 레시피 카테고리
│   ├── youtube.js         # YouTube 분석
│   └── ai.js              # AI 파이프라인
│
├── services/              # 비즈니스 로직
│   ├── aiPipelineService.js    # AI 영상 분석 파이프라인
│   ├── geminiService.js        # Gemini AI 서비스
│   ├── ocrHandler.js           # OCR 처리
│   ├── supabaseClient.js       # Supabase 클라이언트
│   └── supabaseService.js      # Supabase 서비스
│
├── uploads/               # 업로드 파일 저장소
│   └── receipts/         # 영수증 이미지
│
├── video_files/           # 다운로드된 YouTube 영상
│
├── temp_frames/           # 임시 프레임 (OCR 분석용)
│
├── logs/                  # 로그 파일
│
├── combined_sub/          # 병합된 자막 파일
├── whisper_sub/           # Whisper 자막 파일
├── prompt_out/            # AI 프롬프트 출력
├── parsed_out/            # 파싱된 결과
└── result_out/            # 최종 결과
```

### 백엔드 주요 특징
- **RESTful API**: Express 기반 REST API
- **AI 파이프라인**: YouTube 영상 → OCR → Whisper → Gemini 분석
- **파일 시스템**: 임시 파일 관리 및 정리
- **Supabase 연동**: 데이터베이스 및 스토리지 관리

---

## 🔄 프론트엔드 ↔️ 백엔드 통신 구조

```
┌─────────────────────────────────────────────────────────┐
│                    프론트엔드 (CookitMobile)              │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  features/auth/          →  API: POST /api/auth/login   │
│  features/recipe/        →  API: GET /api/recipes        │
│  features/community/     →  API: POST /api/userPosts     │
│  features/refrigerator/  →  API: POST /api/receiptOcr   │
│  features/profile/       →  API: GET /api/users          │
│                                                          │
└─────────────────────────────────────────────────────────┘
                           │
                           │ HTTP/HTTPS
                           │
┌─────────────────────────────────────────────────────────┐
│                    백엔드 (Server)                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  routes/                 →  API 엔드포인트 정의         │
│  services/                →  비즈니스 로직 처리         │
│                           →  Supabase DB 연동            │
│                           →  AI 서비스 연동              │
│                                                          │
└─────────────────────────────────────────────────────────┘
                           │
                           │
┌─────────────────────────────────────────────────────────┐
│                    외부 서비스                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Supabase (DB + Storage)                                 │
│  Google Gemini AI                                         │
│  OpenAI Whisper                                           │
│  YouTube API                                              │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 주요 API 엔드포인트

### 인증
- `POST /api/auth/login` - Google 로그인
- `POST /api/auth/logout` - 로그아웃

### 레시피
- `GET /api/recipes` - 레시피 목록 조회
- `GET /api/recipes/:id` - 레시피 상세 조회
- `POST /api/recipes/:id/view` - 조회수 증가
- `GET /api/recommendations` - 추천 레시피

### 커뮤니티
- `GET /api/userPosts` - 게시글 목록
- `POST /api/userPosts` - 게시글 작성
- `GET /api/comments/:postId` - 댓글 조회
- `POST /api/comments` - 댓글 작성

### 냉장고
- `POST /api/receiptOcr` - 영수증 OCR 분석
- `GET /api/receiptItems` - 냉장고 아이템 조회
- `POST /api/receiptItems` - 아이템 추가

### AI 분석
- `POST /api/ai/analyze` - YouTube 영상 분석
- `GET /api/ai/status/:videoId` - 분석 상태 조회

---

## 🗂️ 데이터베이스 (Supabase)

주요 테이블:
- `users` - 사용자 정보
- `recipes` - 레시피 데이터
- `recipe_stats` - 레시피 통계 (조회수, 좋아요)
- `recipe_likes` - 레시피 좋아요
- `user_posts` - 커뮤니티 게시글
- `post_comments` - 댓글
- `receipt_items` - 냉장고 아이템

---

## 📝 주요 설정 파일

### 프론트엔드
- `babel.config.js` - Babel 설정 (Path Aliases)
- `tsconfig.json` - TypeScript 설정
- `app.config.js` - Expo 앱 설정
- `.env` - 환경 변수 (Supabase URL, API Key)

### 백엔드
- `package.json` - 의존성 및 스크립트
- `nodemon.json` - 개발 서버 설정
- `.env` - 환경 변수 (Supabase, API Keys)

